# j01/secret.example/

Committed template. Copy to `../secret/` (gitignored) when J01-specific
secrets are actually needed. Today J01 has none — it rides entirely on
`zangetsu/secret/.env` for DB / Redis / LLM keys.
