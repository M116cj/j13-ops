# ============================================================
# J01 secret.example — no real values here
# ============================================================
# Copy this file to ../secret/.env (gitignored, 600) and fill only
# if J01 needs strategy-specific secrets. Most runtime env comes from
# zangetsu/secret/.env (DB, Redis, cache).
#
# Placeholder for future strategy-specific env (e.g. per-strategy
# webhook URLs, dedicated monitoring tokens). Leave empty until needed.

# J01_TELEGRAM_THREAD_ID=<ID>
# J01_DEPLOYABLE_APPROVAL_WEBHOOK=<URL>
