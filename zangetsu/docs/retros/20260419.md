# Retro — 2026-04-19（Zangetsu post-mortem + 規範重整 + miniapp 修復）

## 走對的
- Gemini adversarial audit 一次抓出 CRITICAL + HIGH × 3（之前 24h 無人發現）
- SQL 直查 638 筆 champion passport → 發現 0 筆用 indicator terminals（顛覆假設）
- 承認 sign-flip 假設被 SQL 證偽後立即 pivot，沒死守
- 把規範分 6 份 scratch 暫存供 j13 review 而非自改 .claude/ → 尊重敏感權限
- 3-card miniapp 重設計把空殼卡片全砍，scope 對齊 j13 真正需求

## 卡住的
- DEAP 的 addTerminal(callable) 不會自動 invoke — 耗了 3 次 patch 才找到 subclass + mapping swap 方案
- Codex wrapper 在 non-interactive 下 silent fail，浪費一次 dispatch
- Gemini CLI auth 在 Alaya 沒設，走 Mac 版本要 rsync 檔案，增加延遲
- 前序 session「v0.5.1 A4 legit」文字混淆太久，我本 session 接手時也誤信

## Adversarial 抓到的
- Gemini 獨力找出 indicator_cache 根本沒注入（CRITICAL）
- SQL 數據推翻 Claude 的 sign-flip 假設（140 POS_IC vs 7 NEG_IC 比例顛覆）
- 規範草稿 §17.4 auto-revert 的 merge conflict failure mode

## Research 影響
- 2026-04-18 的 feedback_outcome_metric.md 規則警告被 04-19 照樣違反 → 催生 §17.3+17.5 硬 Gate
- DEAP gp 內部 Terminal.format + __slots__ 限制 → 確定 subclass 替換 mapping 是唯一乾淨路徑

## 下次要改的
- Session 開頭強制跑  SQL baseline（§17.1）避免繼承前 session 的「done」謊
- 大型規範修改分拆多 session：文本 session + 實作 session + review session
- Codex CLI 在不可互動模式下先做 smoke test（echo '{}' | codex exec noop），確認 wrapper 會寫檔才真派工
