# Retrospective — 2026-04-22

**Session scope**：7 連續任務（421-3 MR generalization → exception overlay 落地）+ 自我團隊優化 + 環境清理。
**Mode**: Solo（Claude Lead），沒 spawn Gemini / Codex。
**Commit pin**：`f098ead5d9d61e2b407951ffc3006e6b078e2d37` (v0.7.2.2)。

---

## 什麼做對了

1. **跨任務物件重用極高** — bo02 wrapper → meanrev → min_hold → policy → exception 同結構一路演化。避免重新從 0 寫 wrapper 4 次
2. **Telemetry-only rescore** (Wilson trial) = 本 session 最關鍵最優解。省了 1 輪 140-cell rerun，資訊增益 100% 保留
3. **Deterministic critical path 嚴守**（same commit + same session + same data window）— 讓 421-5 結果與 421-3 bit-exact 一致，驗證 policy layer 路由無失真
4. **Policy Layer v0 架構一次到位** — thin decision layer + yaml registry + resolver，分離 decision/execution；future 加 family 只動 registry
5. **Exception overlay 所有硬規範守住** — 顯式 allow_list、雙軌 key、雙軌 expiry、candidate_exception/test 命名不混、§C full verify 表（2/2 hit, 0 unexpected, 138/138 fallthrough）

## 什麼卡住了

1. **MR verdict 初次誤判 MIXED** — j13 糾正為 NO。我對「decision-grade MIXED 需要補償面」的門檻掌握太寬。修正：verdict 規則應 analyzer 機械 assert，不靠我回想。已在 team-playbook.md §"Verdict writing protocol" 固化。
2. **Wilson trial 預測 17 cells，實際 2 cells** — counterfactual 預測只計 wilson<0.52（median 0.37），沒先計 [0.48,0.52) 窄帶分佈。教訓：預測 unlock 數量前，先畫 key variable 分佈。
3. **Recon 重複 7 次** — 每任務都 grep `generate_alpha_signals`、找 shadow runner、找 gate thresholds。修正：已建 `.ops/quick-ref.md`，下次直接讀。
4. **Pre-bash hook 誤擋 4-5 次** — `curl ... || head`、`grep -E 'a|b'` 被當 obfuscated eval。修正：本 session 加白名單。
5. **Calcifer RED 從頭就存在我沒串連** — 我知 RED，但沒把它跟「89 rows 全 ARENA2_REJECTED」連結。修正：team-playbook 加 pre-task status-VIEW-first 協議。
6. **Uncommitted 工作堆積** — 所有產出到 session 結束才開始 commit。修正：end-of-session checklist 強制寫 ADR + commit。

## Adversarial voice 抓到什麼

- **Counterfactual vs 實際 mismatch**：17 預測 → 2 實際。若 j13 直接信 17 決策，會誤判 YES。analyzer 應同時輸出「不同 threshold下的 unlock 數量曲線」，不是單點值。
- **`val_neg_pnl` / `val_low_sharpe` 在 Volume C6 為 null gate**：本 session 才發現，前 421-3 / 421-5 都誤以為三 val gate 對等。ADR 記錄。
- **同 formula 不同 symbol 不是獨立證據**：2 個 allow_list cells 都是 `decay_20(volume)`，是一條 edge 兩處表現。β path 註明此限制。

## 外部 research 如何改變做法

- Prior 421-3 truth（Volume YES、Breakout NO）直接輸入 policy registry，避免重跑
- min_hold_ablation 預先排除「降 min_hold 可能救 MR」這條線，讓本 session 不浪費 tokens 在重測
- bo02 wrapper 作為 meanrev / min_hold / policy wrapper 的結構模板 — 前一天的工作直接加速今日

## 下次要改的一件事

**Verdict 改由 analyzer 機械 assert，不靠 Claude 回想規則。**

具體：每個 decision-grade task 的 analyzer 必須在 stdout 直接印：
```
YES conditions: {rule_1: PASS/FAIL, rule_2: ...}
NO  conditions: {rule_1: PASS/FAIL, ...}
VERDICT: YES / MIXED / NO  (計算後)
```
報告寫作只抄 analyzer 輸出，不重新推論。今日 Wilson trial rescore 已是此範本，MR test 等下次重跑也應 retrofit。

## Team mode 決定（本 session 皆 Solo）

下階段 j13 宣布「大型任務、高強度」。我已建：
- `docs/team/team-playbook.md`（五人分工 + 標準流程）
- `docs/team/spawn-gemini.md`（Gemini 對抗審查模板）
- `docs/team/spawn-codex.md`（Codex scoped exec 模板）

下次任務若：
- 主要實作 > 2 hr 且 cross-module → `/team` 流程
- Verdict 判斷帶歧義 → spawn Gemini 做對抗 review（避免 MR-MIXED 重演）
- 需大量 analyzer / test 撰寫 → delegate Codex

## Open items 進入下一 session

| Item | 等級 |
|---|---|
| P0 Pipeline 停滯（89 rows ARENA2_REJECTED, 0 deployable, 30h+ 無新 row） | 最高 |
| P1 95 staging `pending_validator_error` — admission_validator 拋 exception | 高 |
| P2 Policy layer `entry_threshold=0.90` vs v0.7.2.2 production `0.95` 語義對齊 | 中 |
| wd_keepalive 21h failed — OS 級 watchdog，與 pipeline 無關 | 低 |
| Exception overlay expiry notifier（2026-05-22 review、2026-07-22 fail-close） | 中（cron hook 待建）|

下個 session 開頭的標準動作：status-VIEW 檢查、Calcifer block 檢查、`.ops/quick-ref.md` read、classify complexity。
