# Retro 20260428 — OP1 Primitive Registration

## Went right
- Pre-existing uncommitted +39 LOC diff for OP1 在 alpha_engine.py 已存在；不需重寫，只需驗證+補強
- 9 個 primitives 全部已存在於 `alpha_primitives.py`（line 198/216/387/416/442/463/485/567/635），不需新增 numba 函式
- Lambda capture distinctness 用 `dd=d` 默認參數慣用法 — 一次驗證即過
- Constraint compliance 一次性通過（`NO_FORBIDDEN_TOUCHES` grep）
- Gemini /review 5 維度全跑，僅 1 個有效 nit（_FallbackPrims gap）

## Stalled
- Numba JIT cache 對 sys.path key 敏感；測試 rig 兩次配置才匹配生產 cache（最終 `sys.path = [zangetsu, j13-ops]`）— 浪費 ~3 個來回
- gh search 第一次空回應；切到 `gh api -X GET search/issues` 才正常 — gh CLI 的 search 子命令行為不一致

## Adversarial voice caught
- **_FallbackPrims gap**（Gemini 抓到）：原 +39 LOC 只動 production path，9 個新 primitives 在 fallback 上不存在 → import 失敗時 GP 評估會 silent AttributeError mid-evolution。Q1.3 (External dependency failure) 違反
- 修補：加 217 LOC numpy-only stubs，TEST1 模擬 import failure 確認 graceful

## Research impact
- DEAP issues 集中在 typed GP；本 engine 用 untyped，不適用
- 確認 `dd=d` 是 Python 標準慣用法 — 不是 zangetsu 專屬技巧

## One change next time
- **直接以 import-failure simulation 作 Q1.3 證據**：本次 Q1 預估時把 "External dependency failure" 標 PASS（理由：HAS_PRIMS=True 為現實），但這是把「現實」當「不變式」。Gemini 證明了 fallback 路徑也是 invariant 範疇。下次 Q1.3 應該強制跑「禁用主依賴」的模擬測試，而非默認 happy path。

## Verdict
**COMPLETE_OP1_PRIMITIVES_REGISTERED** — pending j13 final commit authorization
