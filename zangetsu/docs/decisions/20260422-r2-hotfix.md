# R2 Hotfix Application — Stub

**Date:** 2026-04-22 (Asia/Taipei)
**Phase:** Alpha OS 2.0 v3.1 — Track R — Phase R2-N2
**Status:** STUB — Markl to expand after G2 verdict
**Rollback anchor:** `480976c18a94967ab8b467eddc29c7317faffe0e`

## What changed
1. `services/arena_pipeline.py` — `ENTRY_THR 0.95→0.80`, `EXIT_THR 0.65→0.50` (reverts f098ead5 v0.7.2.2). Round log now emits per-reason reject counts every 10 rounds.
2. `services/arena23_orchestrator.py` — CD-14 deep fix: A2 V10 must evaluate on `data_cache[sym]["holdout"]` slice (previously silently used train). Hard-fail on missing holdout. Data cache now loads train + holdout stratified by `TRAIN_SPLIT_RATIO=0.7`. A3 uses train explicitly. Backtester max_hold → `_strategy_max_hold(champion["strategy_id"])`. ARENA2_REJECTED passport_patch → `see_engine_log_for_reject_reason`.

## Not applied here
- SQL migration `v0.7.2.3_admission_duplicate_handling.sql` — already live in DB (verified 2026-04-22T17:33Z; see R2-N1-BEFORE-SNAPSHOT.md §4.1).

## References
- Charter: `docs/decisions/20260422-alpha-os-2.0-charter.md`
- Before snapshot: `~/claude-inbox/retros/zangetsu-recovery-program-v1/phase2/R2-N1-BEFORE-SNAPSHOT.md`
- Patches: `~/claude-inbox/retros/zangetsu-recovery-program-v1/phase2/step0/*.patch`
- Gemini pre-review: YELLOW — APPLY WITH CONDITIONS (3 conditions verified — see R2-N1 bundle).

## Gemini conditions status
- C1 (re-enqueue is UPDATE not RE-INSERT): VERIFIED — worker polls `WHERE status=ARENA1_COMPLETE` via `claim_champion`; no insert path.
- C2 (holdout allows 25 trades): VERIFIED — TRAIN_SPLIT_RATIO=0.7 → holdout 30%; existing alphas' arena1_n_trades ~1076 on train → holdout estimate 150-500, well above 25.
- C3 (2h auto-revert): NOT auto. j13 chose: 2h alert, no auto-revert. Additional alerts: A2 pass rate >30% / few_trades >90%.

## R2 Acceptance (G2) — pending R2-N3/N4
At least 2 of: deployable_count>0 persistent / fresh not 100% ARENA2_REJECTED / pending_validator_error≠95 (already met) / champions_last_1h>0 / last_live_at_age_h interpretable.

## Adversarial open issues to test in R2-N4 window
- Death-location transfer: if fresh status shifts from 100% ARENA2_REJECTED to >30% ARENA2_COMPLETE then >50% ARENA3_REJECTED → flag noise-fitting.
- Multiplicative fragility: narrower holdout × looser signal. If few_trades dominates rejects (>90%) post-fix → data-starvation not signal-filtering.

Markl: expand §"Consequences" / §"Decision history" / §"What we would do differently" after G2 verdict.
